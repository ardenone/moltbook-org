# DO NOT COMMIT - Template for PostgreSQL superuser credentials
# Use this template to create a SealedSecret

apiVersion: v1
kind: Secret
metadata:
  name: moltbook-postgres-superuser
  namespace: moltbook
type: kubernetes.io/basic-auth
stringData:
  username: postgres
  password: "REPLACE_WITH_SECURE_SUPERUSER_PASSWORD"
