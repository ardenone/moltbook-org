---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: moltbook-api
  namespace: moltbook
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`api-moltbook.ardenone.com`)
      kind: Rule
      services:
        - name: moltbook-api
          port: 3000
  tls:
    certResolver: letsencrypt
