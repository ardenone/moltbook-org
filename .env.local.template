# Local Environment Variables for Moltbook Development
# DO NOT COMMIT this file to git - it contains sensitive information
# Copy this file to .env.local and fill in your values
#
# Setup: cp .env.local.template .env.local
# Then: Edit .env.local with your actual values
#
# Usage: source .env.local before running build-images.sh
# Or: GITHUB_TOKEN=$(grep GITHUB_TOKEN .env.local | cut -d '=' -f2) ./scripts/build-images.sh --push

# ============================================================================
# GitHub Container Registry Authentication
# ============================================================================
# Required for pushing container images to ghcr.io via build-images.sh
# Create token at: https://github.com/settings/tokens
# Required scopes: write:packages, read:packages
GITHUB_TOKEN=ghp_your_github_personal_access_token_here

# Optional: GitHub username (defaults to 'github' in build script)
GITHUB_USERNAME=ardenone

# ============================================================================
# Other Development Variables
# ============================================================================

# API Development
# API_PORT=8000
# API_LOG_LEVEL=debug

# Frontend Development
# NEXT_PUBLIC_API_URL=http://localhost:8000
